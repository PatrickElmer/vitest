import { describe, it, expect } from 'vitest';
import { generateRuntimeFunction } from '../../packages/csp/src/parser.js';

describe('First', () => {
    describe('first', () => {
        it('should pass', () => {
            expect(1).toBe(1);
        });
});
